/ = form_for @customer, url: "/customers/#{@customer.id}" do |f|
/ 	= render "form", f: f
/ 	= f.submit "EDIT"



/ = link_to "DELETE",  {:action => "destroy", :id => "#{@customer.id}"}, :method => :delete, :class => "icon-delete", :confirm => "Are you sure"


